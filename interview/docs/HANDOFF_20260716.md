# New Agent 引継ぎ資料（2026-07-16 終了時点）

| 項目 | 内容 |
|---|---|
| 更新 | 2026-07-16 終了時点（P3クローズ後・P4着手前） |
| 対象 | `/opt/interview`（git root は **`/opt`**、パスは `interview/...`） |
| 同時配布 | 本ファイル + `docs/HANDOFF_NEW_AGENT_PROMPT.md`（開始プロンプト） |
| HEAD目安 | `003ac52`（本日最終コミット。`bf41e6a` 以降の本日5コミットを含む） |

---

## 0. 作業開始前チェック（必須）

作業ツリー上で `interview/` 主要ファイルが誤削除され `git status` で `D` になることがある。  
コミット本体には存在するため、**実装の前に復元**すること。

```bash
cd /opt
git status -sb -- interview/
# 削除(D)が出たら:
git checkout HEAD -- interview/
# 確認:
test -f interview/app/judge.py && test -f interview/app/live_bridge.py && echo OK
```

- 破壊的操作（`reset --hard` / `push --force`）禁止
- `/opt` 配下の **他アプリ**（`deploy/` `gemini-ui/` 等）の未コミット変更には触れない
- `interview/certs/` は証明書（gitignore 想定）。コミットしない
- `interview/docs/acceptance/P3/repro_d06/` は D-06 再現スクリプト（退避済み）

---

## 1. 必読順

1. `docs/SPECIFICATION.md` — 仕様正本
2. `docs/CURSOR_INSTRUCTIONS.md` — 絶対制約・フェーズ
3. `docs/acceptance/P3/CLOSE.md` — **P3 クローズの正**（検収作業は完了）
4. `docs/P4_SCOPE.md` — **P4 中核仕様の正**（確定済み・実装は未着手）
5. `docs/CONSENT_REVISION_PROPOSAL.md` — 同意改訂案（**承認待ち**）
6. 矛盾時は SPECIFICATION 優先。迷い → `docs/QUESTIONS.md` に書いて停止（推測実装禁止）

補足: `docs/acceptance/P3/ACCEPTANCE_LOG_20260715.md` は審判差し戻し解除の詳細証跡。P3全体の最終状態は `CLOSE.md` を優先する。

---

## 2. P3 ステータス（2026-07-16・正式クローズ）

出典: `docs/acceptance/P3/CLOSE.md`

| 項目 | 状態 | コミット等 |
|---|---|---|
| P3全体 | **正式クローズ** | `b865386`（CLOSE + D-06再現退避） |
| 音声経路 | **合格**（検収完了） | session 17 実測（7/14） |
| 審判パート | **合格**（差し戻し解除・検収完了） | 7/15 |
| D-07 尺度クランプ | 受入OK・完了 | `021074e` |
| D-08 interrupted マーク | 受入OK・完了 | `828724c` |
| D-06 Live 1007 | **条件付きクローズ**（再現不能・再発時のみ再開） | 調査 `bf41e6a` |

注意: `docs/P3_ACCEPTANCE_REPORT_REQUEST_v2.md` の E 節（審判差し戻し）は **7/14 時点**で古い。検収作業の記述は完了扱いとする。

### D-06 再オープン条件（CLOSE.md より）

- 同種の 1007 エラー再発時のみ調査再開
- 再発時の初手: `live_bridge` への送信ペイロード一時ログ
- 再現スクリプト: `docs/acceptance/P3/repro_d06/`

---

## 3. 本日（2026-07-16）のコミット履歴

`bf41e6a` 以降・本日5件（新しい順）:

| コミット | 内容 |
|---|---|
| `003ac52` | docs(P4): スコープ定義書 + 評価シートPDF格納 + P4-2指示書 |
| `b102021` | feat(P4-1): 採点中ローディング表示（テキスト/音声2経路、失敗時リトライ可） |
| `a7bd1bc` | docs: P4-1 実装指示書 |
| `1996b7e` | docs: 同意文言改訂案（承認待ち） |
| `b865386` | docs(P3): クローズ記録 + D-06再現スクリプト退避 |

---

## 4. P4 現状（仕様確定・実装はこれから）

出典: `docs/P4_SCOPE.md`

| 項目 | 状態 |
|---|---|
| P4スコープ（評価制度接地型面談） | **仕様確定済み**（Q1〜Q12） |
| 同意文言改訂 | **承認待ち**（`CONSENT_REVISION_PROPOSAL.md`）。**承認取得はP4実装開始の前提** |
| P4-1 採点中ローディング | **実装済み**（`b102021`） |
| P4-2 セッションID表示 | **未着手・即実装可**（`docs/P4-2_SESSION_ID_INSTRUCTIONS.md`） |
| P4-0 シートマスタJSON | **次の本実装着手点**（PDF原本から4等級分のJSON設計・作成） |

### 実装フェーズ分割（P4_SCOPE.md 4章）

| フェーズ | 内容 | 状態 |
|---|---|---|
| P4-0 | シートマスタJSON設計・4等級分作成 | **次着手** |
| P4-a | テンプレート登録UI + テキスト面談 + 新審判 | 未着手 |
| P4-b | 音声対応 | 未着手 |
| P4-c | 添削形式 / ローディングは P4-1 で実装済 | ローディング済 |

原本PDF: `docs/reference/` 配下の評価シート4種。

---

## 5. 絶対制約（抜粋）

- Ubuntu `/opt/interview` + venv、純 Python + 外部 API
- 7 等級制、利用者情報に性別なし
- Streamlit 禁止、UI は FastAPI + 静的フロント
- 音声は Gemini Live、モデル名は `.env` の `MODEL_GEMINI_LIVE` のみ（ハードコード禁止）
- `data/rubrics.json` 無断変更禁止
- `.env` は 600・API キー非露出
- **停止指示・ステップ外進行の禁止**（過去に無視→ロールバック方針あり）

### 進行ルール（必須・従来どおり維持）

1. **1操作ずつ**: 指示されたステップ外に勝手に進まない。コミットは人間が明示したときのみ
2. **自己申告は証跡検証**: 「完了」「合格」等の自己申告は、git log / 受入ログ / pytest 等の証跡で検証してから受け入れる
3. 仕様の迷い → 推測実装せず `docs/QUESTIONS.md` に書いて停止

---

## 6. 主要パス

| 領域 | パス |
|---|---|
| Live ブリッジ | `app/live_bridge.py`（`_seed_opening` / sessionResumption） |
| 審判 | `app/judge.py`（D-07 / D-08） |
| セッション | `app/session_service.py`（`_build_judge_transcript`） |
| フロント（P4-1済） | `app/static/app.js` / `index.html` / `style.css` |
| スモーク | `scripts/live_smoke.py` |
| 音声受入スクリプト | `scripts/run_voice_acceptance.py`（合成証跡。実機根拠は session 17） |
| Live 調査 | `docs/notes/live_api_survey.md` |
| D-06 再現 | `docs/acceptance/P3/repro_d06/` |
| 評価シート原本 | `docs/reference/` |
| HTTPS 証明書 | `certs/`（gitignore） |
| systemd | `/etc/systemd/system/interview.service` |

---

## 7. 残件一覧

| 優先 | 項目 | 状態 |
|---|---|---|
| 前提 | 同意文言改訂の社内承認 | **承認待ち**（承認後に index.html / SPEC 改訂） |
| 次本実装 | **P4-0** シートマスタJSON設計・作成 | 未着手 |
| 即実装可 | P4-2 履歴・講評へのセッションID表示 | 指示書あり・未着手 |
| 監視 | D-06 再発時のみ調査再開 | 条件付きクローズ |
| 後続 | score_corrections の DB 永続化 | 未着手 |
| 後続 | CRLF 統一 + `.gitattributes` | 実害なし・別タスク |
| 要確認 | reports 多重 INSERT / session 二重作成 | 以前の指摘・未対応の可能性 |
| 設計課題 | D-09（導入減点）は P4 会話開始ルールで構造解消予定 | P4_SCOPE 6章 |

---

## 8. ドキュメント索引

- `docs/acceptance/P3/CLOSE.md` ← **P3最終状態**
- `docs/acceptance/P3/ACCEPTANCE_LOG_20260714.md` / `ACCEPTANCE_LOG_20260715.md`
- `docs/P4_SCOPE.md` ← **P4仕様正**
- `docs/CONSENT_REVISION_PROPOSAL.md`
- `docs/P4_LOADING_INDICATOR_INSTRUCTIONS.md`（P4-1・実装済）
- `docs/P4-2_SESSION_ID_INSTRUCTIONS.md`（未着手）
- `docs/P3_ACCEPTANCE_REPORT_REQUEST_v2.md`（当時の報告依頼・一部古い）
- `docs/D-07_FIX_INSTRUCTIONS.md` / `docs/D-08_FIX_INSTRUCTIONS.md`
- `docs/CHANGELOG.md`

---

## 9. テスト

- P3クローズ時点・P4-1後とも全体 **46 passed** を維持する想定
- New Agent は復元後に必ず `cd /opt/interview && .venv/bin/pytest -q` を再確認

---

## 10. New Agent の最初の動き

1. git status / 必要なら復元
2. pytest
3. 結果を短く報告し、**人間の次指示を待つ**

### 次回セッションの冒頭タスク（人間指示待ち）

1. **同意改訂の社内承認確認**（`CONSENT_REVISION_PROPOSAL.md`）— 未承認なら同意文・SPECは改訂しない
2. **P4-0 着手**（シートマスタJSON設計）— 承認状況と人間指示に従う

P4-2 は指示書があり即実装可能だが、冒頭優先は上記①②。勝手に着手しない。

開始プロンプト全文: `docs/HANDOFF_NEW_AGENT_PROMPT.md`
