# P4-2: 履歴一覧へのセッションID表示 実装指示書(未着手)

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-07-16 |
| 状態 | 未着手(P4_SCOPE.md 5章参照。実装は後日) |
| 背景 | セッションIDがUI上で確認できず、検証・問い合わせ時にターミナルでのログ確認が必要(2026-07-16 指摘) |
| 対象 | フロントのみ(app/static/)。バックエンド変更禁止 |

## 要件
1. 履歴一覧(s05 #history-list)の各行の行頭に「#ID」を表示する(例:「#17 …」)
   - GET /api/users/{user_id}/sessions のレスポンスに含まれる既存のidフィールドを使用(API変更不可)
2. 講評画面(s04)にも現在のセッションIDを小さく表示する(検証時の突合用)
3. 既存の履歴行の情報・クリック挙動は変更しない

## 変更対象ファイル(これ以外の変更は禁止)
- app/static/app.js
- app/static/index.html(s04へのID表示要素が必要な場合のみ)
- app/static/style.css(体裁調整が必要な場合のみ)

## 受入基準
1. pytest 46件パス維持
2. 履歴一覧の各行に #ID 表示(実機確認)
3. 講評画面にセッションID表示(実機確認)
4. git diff が変更対象ファイルのみ
