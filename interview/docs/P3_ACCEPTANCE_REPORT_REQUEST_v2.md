# P3検収準備指示 v2（報告のみ・実装変更禁止）

回答期限内に、以下を証跡付きで文書回答すること。コード・設定・テストの変更は一切禁止。
変更が必要と考える場合も、まず本報告書で提案として記載し、承認を待つこと。

## A. 既存指示（未回答分・再掲）
1. `grep -rn "器具高"` の全ヒットを報告し、「器具高ぁぁ=マニュアル引用可」と判定した根拠を提示すること
   （引用可否の最終判断は検収者がマニュアル原本と突き合わせて行う）
2. バージイン証跡が実ブラウザ由来か合成かを、取得方法とともに明示すること
3. サーバー証明書のSANに 172.16.16.10 が含まれるかを `openssl x509 -text` の出力で示すこと
4. 追加された音声経路テスト6件の名称と検証内容を列挙すること

## B. 新規報告事項（2026-07-14 実機検収で発見）
5. 【D-06・欠陥】テキスト履歴が存在するセッションを音声モードで開始すると、
   `_seed_opening` の send_client_content 実行後に Gemini Live API が
   1007 (invalid argument) で切断され、音声接続が確立しない（画面は「待機」のまま）。
   純音声の新規セッションでは発生しない。session_id=14、15:01:27 JST のjournalに
   Traceback あり。原因の特定と修正方針の提案を報告すること（修正実施は禁止）
6. reportsテーブルに同一session_idの重複行がある（session 11=2件、session 12=3件）。
   講評再生成の仕様か欠陥かを、該当コードパスを示して説明すること
7. sessions 13・14 が3秒差で連続作成されている。フロントの二重POSTの可能性を含め
   原因を説明すること
8. 音声セッションの最初のpartnerターンのみ audio_flag=0（冒頭挨拶が音声再生されない）。
   仕様か欠陥かを説明すること
9. 音声入力の文字起こし誤認識（例: 意図と異なる語への変換、他言語文字の混入）が
   審判の評価対象テキストに直接入る。現行実装での緩和策の有無と、
   制約事項としてREADMEに明記すべき内容の案を報告すること

## C. 回答形式
- 各項目に対し「事実 → 根拠（ファイル・行番号・コマンド出力）→ 見解」の順で記載
- 出力は docs/acceptance/P3/CURSOR_REPORT_v2.md に保存

## D. 追加報告事項（2026-07-14 審判検証で発見・報告のみ・実装変更禁止）
10. 【D-07・欠陥】審判スコアがルーブリック尺度（3〜7）を逸脱するケースがある。
    実例: session 9 に 1、session 12 の再講評に 0、session 16 は 7項目中6項目が 1〜2。
    text/voice 両モードで発生。judge.py のプロンプトおよび出力バリデーションにおいて
    尺度がどのように強制されているか（されていないか）を、該当コードを示して説明すること
11. 【D-08・欠陥】審判が トランスクリプトに存在しない発話を評価根拠にしている。
    実例: session 10 の good_points に「部下が目標を受け入れた際に承認と励まし…」とあるが、
    当該面談では部下の受諾発話は存在しない（最終AI応答は完全打ち切り、played=""）。
    goal_level_reached=true の判定根拠も同様に不明。審判への入力に interrupted 打ち切り後の
    未再生テキストが含まれていないか、含まれている場合の経路を特定して報告すること
12. 同一セッションの再講評で評価が変動する（session 11: B→C、session 12: A/B/B）。
    講評の再生成が可能な操作経路と、生成パラメータ（temperature等）の現行値を報告すること。
    緩和策（複数回評価の集約等）は提案のみとし、実装しないこと

## E. P3検収ステータス（検収者判断・2026-07-14）
- 音声経路パート: 合格（実機バージイン受入テスト①〜④ すべてOK、session 17 で実測）
- 審判パート: 差し戻し（D-07・D-08 の決着まで P3 クローズ保留）
- 本文書 A〜D の全項目について、証跡付き報告を docs/acceptance/P3/CURSOR_REPORT_v2.md に
  まとめること。実装・設定・テストの変更は一切禁止（提案は文書内でのみ行うこと）
