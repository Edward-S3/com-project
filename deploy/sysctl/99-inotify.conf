# 多数の Streamlit / systemd サービス稼働時の inotify 枯渇対策
# (journalctl -f や systemctl restart 時の "No space left on device" 警告を防ぐ)
fs.inotify.max_user_watches = 524288
fs.inotify.max_user_instances = 1024
fs.inotify.max_queued_events = 32768
